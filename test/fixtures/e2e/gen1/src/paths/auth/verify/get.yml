summary: "Show verify"
responses:
  "404":
    description: "Verify not found"
  "200":
    description: "Success"
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/verify/model.yml"
