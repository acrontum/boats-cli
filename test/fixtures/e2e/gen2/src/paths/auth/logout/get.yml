summary: "Show logout"
responses:
  "404":
    description: "logout not found"
  "200":
    description: "Success"
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/Logout"
